// CONTENT

$menu-width = 250px

#header
  padding: $margin-basic

#header-name
  height: $margin-mid

#header
  width: $menu-width

$main-content-offset = $margin-basic + $margin-mid

#main-content-container
  width: 'calc(100vw - %s)' % ($menu-width + $margin-basic)
  left: $menu-width + $margin-basic
  padding-top: $main-content-offset

#main-content
  width: 410px

// ******************************************************

// MINIMAP

#minimap
  position: fixed
  bottom: $margin-basic
  left: $margin-basic
